/**
 * Renders the layout for displaying character details.
 *
 * @param {React.ReactNode} children - The content to be displayed within the layout.
 * @return {React.ReactNode} The rendered layout.
 */
const CharacterDetailsLayout = ({
	children,
}: {
	children: React.ReactNode
}): React.ReactNode => children

export default CharacterDetailsLayout
